[project]
name = "voice-ai-agent-demo"
version = "0.2.0"
description = "Local voice-agent framework demo with provider boundaries, tracing, batch scenarios, and reports."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Yashowardhan Singh Tomar" }]
dependencies = []

[project.scripts]
voice-agent-demo = "voice_agent_demo.cli:main"

[tool.setuptools]
packages = ["voice_agent_demo"]
